import { StyleSheet, StatusBar } from "react-native";

const styles = StyleSheet.create({
  image: {
    width: 119,
    height: 180,
    borderRadius: 5,
    margin: 5,
  },
  title: {
    fontSize: 20,
    fontWeight: "bold",
  },
});

export default styles;
